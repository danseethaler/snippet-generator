'use babel';

import SnippetGeneratorView from '../lib/snippet-generator-view';

describe('SnippetGeneratorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
